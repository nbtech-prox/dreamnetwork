version: "3.9"

x-dns: &dns
  dns:
    - 8.8.8.8
    - 1.1.1.1

services:
  postgres:
    <<: *dns
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: dreamnetwork
      POSTGRES_USER: dreamuser
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dream_pass_dev}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dreamuser -d dreamnetwork"]
      interval: 5s
      timeout: 5s
      retries: 5

  neo4j:
    <<: *dns
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-dream_neo4j_dev}
      NEO4J_PLUGINS: '["apoc"]'
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4jdata:/data
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p ${NEO4J_PASSWORD:-dream_neo4j_dev} 'RETURN 1'"]
      interval: 10s
      timeout: 10s
      retries: 10

  redis:
    <<: *dns
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    <<: *dns
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://dreamuser:${POSTGRES_PASSWORD:-dream_pass_dev}@postgres:5432/dreamnetwork
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-dream_neo4j_dev}
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: ${SECRET_KEY:-super-secret-key-change-in-prod}
      JWT_EXPIRY_HOURS: 24
      BACKEND_CORS_ORIGINS: ${BACKEND_CORS_ORIGINS:-http://localhost:3000}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  celery-worker:
    <<: *dns
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://dreamuser:${POSTGRES_PASSWORD:-dream_pass_dev}@postgres:5432/dreamnetwork
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-dream_neo4j_dev}
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: ${SECRET_KEY:-super-secret-key-change-in-prod}
    depends_on:
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: celery -A app.celery_worker worker --loglevel=info --concurrency=2

volumes:
  pgdata:
  neo4jdata:
